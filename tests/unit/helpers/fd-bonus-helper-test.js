
import { fdBonusHelper } from 'dv-web-statcard/helpers/fd-bonus-helper';
import { module, test } from 'qunit';

module('Unit | Helper | fd bonus helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fdBonusHelper([42]);
  assert.ok(result);
});

