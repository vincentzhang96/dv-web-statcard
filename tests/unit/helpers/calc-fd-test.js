
import { calcFd } from 'dv-web-statcard/helpers/calc-fd';
import { module, test } from 'qunit';

module('Unit | Helper | calc fd');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = calcFd([42]);
  assert.ok(result);
});

