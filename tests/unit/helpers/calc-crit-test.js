
import { calcCrit } from 'dv-web-statcard/helpers/calc-crit';
import { module, test } from 'qunit';

module('Unit | Helper | calc crit');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = calcCrit([42]);
  assert.ok(result);
});

