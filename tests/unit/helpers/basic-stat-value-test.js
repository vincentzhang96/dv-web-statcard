
import { basicStat } from 'dv-web-statcard/helpers/basic-stat';
import { module, test } from 'qunit';

module('Unit | Helper | basic stat');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = basicStat([42]);
  assert.ok(result);
});

