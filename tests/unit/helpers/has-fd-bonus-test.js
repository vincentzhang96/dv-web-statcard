
import { hasFdBonus } from 'dv-web-statcard/helpers/has-fd-bonus';
import { module, test } from 'qunit';

module('Unit | Helper | has fd bonus');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hasFdBonus([42]);
  assert.ok(result);
});

