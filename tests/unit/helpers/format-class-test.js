
import { formatClass } from 'dv-web-statcard/helpers/format-class';
import { module, test } from 'qunit';

module('Unit | Helper | format class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatClass([42]);
  assert.ok(result);
});

