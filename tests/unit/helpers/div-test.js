
import { div } from 'dv-web-statcard/helpers/div';
import { module, test } from 'qunit';

module('Unit | Helper | div');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = div([42]);
  assert.ok(result);
});

