
import { calcDef } from 'dv-web-statcard/helpers/calc-def';
import { module, test } from 'qunit';

module('Unit | Helper | calc def');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = calcDef([42]);
  assert.ok(result);
});

