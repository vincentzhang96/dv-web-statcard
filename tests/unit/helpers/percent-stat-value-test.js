
import { percentStatValue } from 'dv-web-statcard/helpers/percent-stat-value';
import { module, test } from 'qunit';

module('Unit | Helper | percent stat value');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = percentStatValue([42]);
  assert.ok(result);
});

